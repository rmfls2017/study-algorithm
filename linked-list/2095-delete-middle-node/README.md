# LeetCode 2095: Delete the Middle Node of a Linked List

> 문제 해결 과정을 추론과 함께 정리한 학습 노트

## 📋 문제 정의

**문제**: 연결 리스트의 head가 주어지면 중간 노드를 삭제하고 수정된 리스트의 head를 반환하라.

**중간 노드 정의**: 크기가 n인 연결 리스트에서 `⌊n / 2⌋` 번째 노드 (0-based index)

```
n = 1, 2, 3, 4, 5 → 중간 인덱스 = 0, 1, 1, 2, 2
```

**예시**:
```
입력:  1 → 3 → 4 → 7 → 1 → 2 → 6  (n=7)
index: 0   1   2   3   4   5   6
                   ↑
              middle = 3

출력:  1 → 3 → 4 → 1 → 2 → 6
```

**제약 조건**:
- 노드 개수: 1 ~ 10^5
- 노드 값: 1 ~ 10^5

---

## 🧠 추론 과정: 문제를 처음 만났을 때

### Stage 1: 문제를 내 언어로 다시 정의하기

문제를 읽고 스스로에게 질문을 던진다:

```
Q1. "중간"이 뭐지?
    → n/2 번째 (0-based index, 내림)

Q2. "삭제"하려면 뭐가 필요하지?
    → Singly Linked List에서는 이전 노드가 필요하다!
    → target.prev.next = target.next 형태로 건너뛰기

Q3. 내가 알아야 하는 것은?
    → 리스트의 길이, 중간 위치, 그리고 중간의 이전 노드
```

### Stage 2: 가장 단순한 해결책 (Two-Pass)

**직관적 접근**: "길이를 알면 중간을 알 수 있다"

```
1단계: 전체 순회해서 길이 n 구하기
2단계: n/2 위치까지 다시 이동
3단계: 삭제
```

→ [TwoPassSolution.java](./TwoPassSolution.java) 참고

**자기 검증**:
- ✅ 정확성: 동작함
- ❌ 효율성: 두 번 순회 필요 → 개선 가능?

### Stage 3: "더 나은 방법이 있을까?"

**관찰**: 두 번 순회하는 이유가 뭐지?
- 첫 번째 순회: 전체 길이를 모르니까
- 두 번째 순회: 중간 위치를 찾으려고

**핵심 질문**: 
> "길이를 모르는 상태에서 중간을 찾을 수 있을까?"

### Stage 4: 패턴 연결 - Fast & Slow Pointer

**비유**: 마라톤에서 2배 빠른 사람은 같은 시간에 2배 거리를 간다.

```
속도 관계:
- slow: 한 칸씩 이동 (속도 1)
- fast: 두 칸씩 이동 (속도 2)

결과:
- fast가 끝에 도달 = n 이동
- slow의 이동 거리 = n/2 = 중간!
```

**시각화**:
```
1 → 3 → 4 → 7 → 1 → 2 → 6 → null
↑   ↑
S   F   (시작)

1 → 3 → 4 → 7 → 1 → 2 → 6 → null
    ↑       ↑
    S       F   (1회 이동)

1 → 3 → 4 → 7 → 1 → 2 → 6 → null
        ↑           ↑
        S           F   (2회 이동)

1 → 3 → 4 → 7 → 1 → 2 → 6 → null
            ↑               ↑
            S               F   (3회 이동, F가 끝 도달)
            ↑
         중간 노드!
```

### Stage 5: 삭제를 위한 트릭

**문제 발견**: slow가 중간을 가리키면, 어떻게 삭제하지?

Singly Linked List에서 노드를 삭제하려면 **이전 노드**가 필요하다:
```java
prev.next = prev.next.next  // target을 건너뛰기
```

**해결책**: slow가 중간의 **한 칸 전**에서 멈추게 하면 된다!

**방법**: Dummy Node 사용
```java
ListNode dummy = new ListNode(0, head);
ListNode slow = dummy;  // slow를 한 칸 뒤에서 시작
ListNode fast = head;
```

→ [Solution.java](./Solution.java) 참고

---

## 📊 해결책 비교

| 방식 | 시간 | 공간 | 순회 횟수 | 코드 |
|------|------|------|-----------|------|
| Two-Pass | O(n) | O(1) | ~1.5n | [TwoPassSolution.java](./TwoPassSolution.java) |
| Fast & Slow | O(n) | O(1) | ~0.5n | [Solution.java](./Solution.java) |

---

## 🔄 패턴 확장: 뒤에서 k번째 노드 삭제

### 추론 과정

**질문**: "뒤에서부터 순회하면 되지 않을까?"

**제약 인식**: Singly Linked List에서는 `next`만 있어서 뒤로 갈 수 없다!

**새로운 관점**: "앞에서부터 가면서, 뒤에서의 위치를 알 수 있을까?"

**핵심 통찰**: 두 포인터 사이에 **간격 k**를 유지하면 된다!

```
뒤에서 k번째를 찾고 싶다면:
    → 끝에서 k칸 떨어진 위치
    → first가 k칸 먼저 출발
    → 함께 이동하면 간격 k 유지
    → first가 끝에 도달할 때, second는 끝에서 k칸 전!
```

**시각화** (k=2):
```
[1] → [2] → [3] → [4] → [5] → null
 ↑           ↑
 S           F     (간격 = 2 유지)

[1] → [2] → [3] → [4] → [5] → null
       ↑           ↑
       S           F     (함께 이동)

[1] → [2] → [3] → [4] → [5] → null
             ↑           ↑
             S           F     (함께 이동)

[1] → [2] → [3] → [4] → [5] → null
                   ↑           ↑
                   S         F=null (종료!)

S = [4] = 뒤에서 2번째!
```

### 코드 비교

| 문제 | 관계 | 핵심 코드 |
|------|------|-----------|
| 중간 노드 | 속도 비율 2:1 | `fast = fast.next.next` |
| 뒤에서 k번째 | 간격 k 유지 | `first`가 k칸 먼저 출발 후 함께 이동 |

---

## 🔗 심화: Doubly Linked List에서의 삭제

### 구조

```java
class DoublyListNode {
    int val;
    DoublyListNode prev;  // 이전 노드 참조
    DoublyListNode next;  // 다음 노드 참조
}
```

### 삭제 로직

```
Before:  [A] ⇄ [target] ⇄ [B]
After:   [A] ⇄ [B]
```

```java
target.prev.next = target.next  // A → B 연결
target.next.prev = target.prev  // B ← A 연결
```

### Edge Case 처리

| target 위치 | 문제 | 해결 |
|-------------|------|------|
| head | `target.prev` = null | 조건부 실행 or Dummy |
| tail | `target.next` = null | 조건부 실행 or Dummy |

**해결책 1: 조건부 실행**
```java
if (target.prev != null) {
    target.prev.next = target.next;
}
if (target.next != null) {
    target.next.prev = target.prev;
}
```

**해결책 2: Dummy Node**
```
[dummyHead] ⇄ [1] ⇄ [2] ⇄ [3] ⇄ [dummyTail]
```
→ 모든 노드가 양쪽에 무언가를 가지므로 조건 분기 불필요

---

## 🎯 핵심 정리

### Two Pointer 패턴

```
문제 유형              →  두 포인터의 관계
─────────────────────────────────────────
중간 찾기              →  속도 차이 (2:1)
뒤에서 k번째 찾기       →  간격 유지 (k칸)
사이클 찾기            →  속도 차이 (결국 만남)
```

### Dummy Node의 가치

- **head 처리 단순화**: head가 삭제 대상일 때도 동일한 로직
- **null 체크 제거**: 항상 prev/next가 존재
- **코드 일관성**: 모든 케이스를 하나의 로직으로 처리

### 추론의 핵심 질문들

1. "이 문제의 제약 조건은 무엇인가?" (Singly → 뒤로 못 감)
2. "가장 단순한 해결책은?" (Two-Pass)
3. "더 나은 방법이 있을까?" (One-Pass with Two Pointers)
4. "Edge case는 무엇인가?" (n=1, head/tail 삭제)
5. "이 패턴을 다른 문제에 적용할 수 있을까?" (뒤에서 k번째)

---

## 📚 관련 문제

| 문제 | 핵심 패턴 |
|------|-----------|
| [876. Middle of the Linked List](https://leetcode.com/problems/middle-of-the-linked-list/) | Fast & Slow 기초 |
| [19. Remove Nth Node From End of List](https://leetcode.com/problems/remove-nth-node-from-end-of-list/) | 간격 k 유지 |
| [141. Linked List Cycle](https://leetcode.com/problems/linked-list-cycle/) | 사이클 탐지 |
| [142. Linked List Cycle II](https://leetcode.com/problems/linked-list-cycle-ii/) | 사이클 시작점 |
| [234. Palindrome Linked List](https://leetcode.com/problems/palindrome-linked-list/) | 중간 찾기 + 뒤집기 |

---

## ✏️ Self-Check 질문

1. fast가 `null`이 아니라 `fast.next`도 체크하는 이유는?
2. n=5일 때 while 루프는 몇 번 실행되는가?
3. dummy node 없이 구현하려면 어떻게 해야 하는가?
4. Doubly Linked List에서 뒤에서부터 접근하면 시간복잡도가 어떻게 되는가?